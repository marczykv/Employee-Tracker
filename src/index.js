const inquirer =  require('inquirer');
const queries = require('./queries');




const runApp = () => {
    // Use inquirer to present the user with choices and then call the respective function.
};

runApp();